# Secrets / credentials — never commit
.env
agent_config.yaml

# Python / uv
.venv/
__pycache__/
*.pyc
.uv/
# uv.lock is intentionally committed — required for reproducible `uv sync --frozen` in Docker/CI

# Cached graph comprehension briefings (regenerated from the live graph)
.cache/
